# 健康档案小程序设计契约

## 产品定位

这是一个给个人和家庭成员使用的健康记录工具。用户常见使用场景是在医院候诊、复诊前、体检后整理报告时快速查看记录，所以界面要安静、清晰、可扫描，不做营销感或装饰感。

## 视觉方向

- 风格：医疗工具型、克制、可信、移动端优先。
- 主色：蓝绿色 `#72b0be`，用于主操作、选中状态和时间轴节点。
- 信息色：浅蓝 `#cde9f2`，用于搜索、对比报告和识别信息等辅助入口。
- 健康色：浅绿 `#d6ebb4`，用于用药情况、身体不适、健康状态和积极建议。
- 页面底色：浅灰 `#efefef`，内容卡片保持白色，形成清晰层级。
- 危险色：红色 `#b42318`，只用于删除和不可恢复操作。
- 背景：页面使用 `#f7f9fb`，内容面板使用白色，避免深色大面积背景影响医疗信息阅读。

## 排版

- 使用系统字体，保证微信小程序中稳定显示。
- 标题强调信息层级，不追求海报感。
- 数据、日期、指标优先保持短行和可扫读。
- 正文最小字号保持可读，不使用过浅灰色承载重要信息。

## 布局

- 顶部区域承载当前成员、搜索和主要入口。
- 记录页以时间轴为主，不增加过多分散入口。
- 档案页使用可点击列表卡片，成员选择和进入详情要清楚。
- 记录编辑页按“类型 → 附件 → 基础信息 → 记录内容”组织。
- 体检对比页以“选择报告 → 指标表格”两段式组织。

## 组件规则

- 按钮高度不低于 `52rpx`，关键按钮不低于 `68rpx`。
- 删除操作必须明确显示文字，并通过确认弹窗执行。
- 状态不能只靠颜色表达，必须配合文字，如“已识别”“待识别附件”“未识别指标”。
- 卡片圆角保持 `8rpx`，避免过度圆润。
- 阴影只用于可点击卡片或浮层，不做装饰性漂浮。

## 交互规则

- 上传附件后要保留标题、日期、识别文字和预览入口。
- OCR 识别失败时要给出可恢复路径：重新识别或手动修正文字。
- 对比功能只展示识别到的可对比指标，不要求用户重复填写医院、科室、医生。
- 复杂功能要先给简短说明，再让用户操作。

## 禁止项

- 不使用大面积紫蓝渐变、玻璃拟态、装饰光斑。
- 不做营销落地页式 hero。
- 不用图标替代关键医疗操作文字。
- 不让文字溢出按钮、卡片或表格单元。
